<%= form_for(@auto_bid) do |f| %>
  <% if @auto_bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auto_bid.errors.count, "error") %> prohibited this auto_bid from being saved:</h2>

      <ul>
      <% @auto_bid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trade_id %><br>
    <%= f.number_field :trade_id %>
  </div>
  <div class="field">
    <%= f.label :max_bid %><br>
    <%= f.number_field :max_bid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
